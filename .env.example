# Fictional-data local pilot. Never put real patient records in this prototype.
PORT=4317
OLLAMA_MODEL=qwen3:1.7b
# Optional real email delivery. With no SMTP_HOST, reminders stay in the local inbox.
# Use an app-specific password where your email provider requires one.
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=
